name: dev-deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: 廃止ワークフロー案内
        run: |
          echo "[情報] dev-deploy.yml は廃止されています。"
          echo "dev-deploy-v2.yml を使用してください。"
