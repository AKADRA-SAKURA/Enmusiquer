name: dev-deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow notice
        run: |
          echo "[INFO] dev-deploy.yml is deprecated."
          echo "Use dev-deploy-v2.yml instead."